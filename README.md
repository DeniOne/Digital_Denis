# 🧠 Digital Denis

**Personal Cognitive Operating System** — система цифрового двойника для усиления когнитивных процессов.

---

## 🚀 Быстрый старт

### Требования
- Docker & Docker Compose
- Python 3.11+
- Node.js 18+

### Локальный запуск

```bash
# 1. Скопируйте конфигурацию
cp .env.example .env
# Отредактируйте .env — добавьте API ключи

# 2. Запустите инфраструктуру
docker-compose up -d postgres redis

# 3. Запустите Backend
cd backend
python -m venv venv
.\venv\Scripts\activate  # Windows
pip install -r requirements.txt
uvicorn main:app --reload

# 4. Запустите Frontend
cd frontend
npm install
npm run dev

# 5. Запустите Telegram Bot (опционально)
cd telegram
.\venv\Scripts\activate
pip install -r requirements.txt
python bot.py
```

### Доступ
- **Web UI:** http://localhost:3000
- **API Docs:** http://localhost:8000/docs
- **Telegram:** @YourBotName

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────┐
│              INTERFACE LAYER                     │
│   Telegram Bot  │  Web UI  │  Future APIs       │
└────────────────────────┬────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────┐
│           BACKEND ORCHESTRATOR                   │
│   Request Router │ Digital Profile │ Context    │
└────────────────────────┬────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────┐
│              AGENT LAYER                         │
│   Core │ Analyst │ Operator │ Memory            │
└────────────────────────┬────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────┐
│              MEMORY LAYER                        │
│   Redis (Short) │ PostgreSQL (Long) │ Vector   │
└─────────────────────────────────────────────────┘
```

---

## 📁 Структура проекта

```
digital-denis/
├── backend/          # FastAPI Backend
│   ├── agents/       # Core, Memory агенты
│   ├── api/          # REST endpoints
│   ├── memory/       # Short/Long-term memory
│   ├── orchestrator/ # Request Router, Profile
│   └── llm/          # LLM провайдеры
├── frontend/         # Next.js UI
├── telegram/         # Telegram Bot
├── ai/profiles/      # Digital Profile (YAML)
└── docs/             # Документация по фазам
```

---

## 🛠️ Tech Stack

| Компонент | Технология |
|-----------|------------|
| Backend | Python, FastAPI |
| Frontend | Next.js, TypeScript, Tailwind |
| Database | PostgreSQL, Redis |
| LLM | OpenRouter, Groq |
| Bot | python-telegram-bot |

---

## 📚 Документация

Полная документация разбита на фазы: [docs/README.md](docs/README.md)

---

## 📜 Лицензия

Private / Custom License
